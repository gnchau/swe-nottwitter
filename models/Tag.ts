/**
 * @file A Tag that Users add to Tuits.
 */

/**
 * @class Tag represents a tag that a Tuit may include.
 * Tags are custom categories that any user can create by adding
 * a hash sign (#) in front of any word in their Tuit.
 * @property {string} tag represent the Tag name
 */

export default class Tag {
    private tag: string = '';
}